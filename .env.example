# LLM provider: "openai" (default, needs OPENAI_API_KEY) or "mock" (deterministic, no key)
LLM_PROVIDER=openai
OPENAI_API_KEY=
# Optional override; default is gpt-4o-2024-08-06 per docs/system-design.md
OPENAI_MODEL=
